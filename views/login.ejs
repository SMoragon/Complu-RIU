<!DOCTYPE html>
<html lang="en">

    <head>
        <%- include ('pieces/head')%>
    </head>

    <body>
        <header>
            <!-- Include pieces of header -->
            <%- include ('pieces/header')%>
        </header>

        <main id="main">
            <!-- Login Form -->
            <div class="container">
                <form id="login_form" method="POST" action="/login">
                    <!-- Email field -->
                    <div class="form-group row">

                        <label for="user_email"
                            class="offset-md-1 col-md-4 offset-sm-2 offset-2 col-10 col-form-label login_field">
                            Correo electrónico: </label>

                        <div
                            class="offset-md-0 col-md-6 offset-sm-1 col-sm-7 offset-1 col-10">

                            <input type="text"
                                class="form-control form-control-sm reg_log_input"
                                name="user_email" id="user_email"
                                value= <%=locals.body? body["user_email"]:""%>>

                            <span class="popup_form_error text-danger"
                                id="mail_error">El
                                email introducido no es correcto. El formato
                                debe
                                ser "abcde@fghi.jkl".</span>

                            <% if (locals.errors && locals.errors["user_email"])
                            { %>
                            <span
                                class="text-danger popup_form_error"
                                style="display: inline-block;">
                                <%= errors["user_email"] %>
                            </span>
                            <% } %>

                        </div>
                    </div>
                    <!-- Password Field -->
                    <div class="form-group row">
                        <label for="user_password"
                            class="offset-md-1 col-md-4 offset-sm-2 offset-2 col-10 col-form-label login_field">
                            Contraseña: </label>
                        <div
                            class="offset-md-0 col-md-6 offset-sm-1 col-sm-7 offset-1 col-10">

                            <input type="password"
                                class="form-control form-control-sm reg_log_input"
                                name="user_password" id="user_password">

                            <div class="popup_form_error text-danger"
                                id="empty_error">Todos
                                los campos deben estar rellenos.</div>
                            <% if (locals.errors)
                            { %>
                            <span
                                class="text-danger popup_form_error d-inline-block"
                                >
                                <%= errors %>
                            </span>
                            <% } %>

                        </div>
                    </div>
                    <!-- After check the Email and Password it´s exist on our databases 
                    and if it doesn´t exist it shows an error message -->

                    <div class="send_buttons_cont">
                        <!-- Submit form button -->
                        <input type="submit" value="ENVIAR"
                            class="send_buttons send_but"></input>
                    <!-- Close form button and redirect to the main page -->
                    <button type="button" class="send_buttons"
                        onclick="closeAndRedirect('/')">
                        CERRAR</button>
                </div>
                <!-- Link that´s take you into a register page -->
                <a href="/register">¿Aún no tienes una cuenta? Regístrate aquí</a>
            </div>
        </main>

        <footer>
            <!-- Include pieces of footer -->
            <%- include ('pieces/footer')%>
        </footer>

    </body>
    <!-- Our JavaScripts validation -->
    <script src="/scripts/loginValidation.js"></script>
    <!-- A little JavaScripts that catch an submit event and validate on client, if the validation fails, then stop the submit -->
    <script>
    document.getElementById('login_form').addEventListener('submit', function (event) {

        if (!validate()) {
            event.preventDefault();
        }
    });

</script>

</html>